import { BadRequestException, ForbiddenException, Injectable, Logger, NotFoundException, OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import type { WriteBatch } from 'firebase-admin/firestore';
import { verifyMessage, getAddress, type Address } from 'viem';
import { FirestoreService } from '../../common/firestore.service';
import { SportmonksDataService } from '../sportmonks/sportmonks-data.service';
import { SubscriptionService } from '../subscription/subscription.service';
import { OnchainPredictionService } from '../onchain/onchain-prediction.service';
import { SubmitPredictionDto } from './prediction.dto';

const FIVE=5, TEN=10, TOTAL=50, WINDOW=300;
const SHARES:Record<number,number>={5:50,4:30,3:20};
const BATTERS=['babar azam','mohammad rizwan','virat kohli','rohit sharma','shubman gill','yashasvi jaiswal','kl rahul','suryakumar yadav','jos buttler','travis head','steve smith','kane williamson','rachin ravindra','david warner','glenn maxwell','phil salt','harry brook','joe root','quinton de kock','heinrich klaasen','aiden markram','fakhar zaman','imam-ul-haq','saim ayub','shai hope','litton das','pathum nissanka','kusal mendis','devon conway','marnus labuschagne'];
const BOWLERS=['shaheen afridi','jasprit bumrah','mohammed siraj','mitchell starc','pat cummins','josh hazlewood','trent boult','tim southee','rashid khan','kagiso rabada','anrich nortje','keshav maharaj','mustafizur rahman','taskin ahmed','wanindu hasaranga','mark wood','jofra archer','adil rashid','haris rauf','naseem shah','shaheen','bumrah','starc','rabada'];
type Q={id:string;kind:string;title:string;prompt:string;options:{value:string;label:string}[];featuredPlayer?:any;teamId?:number;correctAnswer?:string};
const arr=(v:any)=>Array.isArray(v)?v:Array.isArray(v?.data)?v.data:[];
const pname=(p:any)=>String(p?.fullname??p?.player?.fullname??`${p?.firstname??p?.player?.firstname??''} ${p?.lastname??p?.player?.lastname??''}`).trim()||`Player ${Number(p?.player_id??p?.id??0)}`;
const terminal=(s:any)=>{const x=String(s??'').toLowerCase();return x.includes('finish')||x.includes('aband')||x.includes('cancel');};
const voided=(s:any)=>{const x=String(s??'').toLowerCase();return x.includes('aband')||x.includes('cancel');};

@Injectable()
export class PredictionService implements OnModuleInit,OnModuleDestroy{
 private readonly logger=new Logger(PredictionService.name);private timer:ReturnType<typeof setInterval>|null=null;private busy=new Set<string>();
 constructor(private readonly db:FirestoreService,private readonly sportmonks:SportmonksDataService,private readonly subscriptions:SubscriptionService,private readonly onchain:OnchainPredictionService){}
 onModuleInit(){void this.sweep();this.timer=setInterval(()=>void this.sweep(),30000);}onModuleDestroy(){if(this.timer)clearInterval(this.timer);this.timer=null;}
 private mref(id:string){return this.db.db.collection('predictionMatches').doc(id);}private eref(id:string){return this.db.db.collection('predictionEntries').doc(id);}private aref(id:string){return this.db.db.collection('predictionAnswers').doc(id);}private wref(m:string,w:string){return this.db.db.collection('predictionWalletLocks').doc(m+'_'+w.toLowerCase());}
 private async read(id:string){const s=await this.mref(id).get();return s.exists?({id:s.id,...s.data()} as any):null;}
 private async sub(uid:string){const s=await this.subscriptions.status(uid);if(!s.active)throw new ForbiddenException('An active CrickX weekly subscription is required to participate in predictions. Subscribe for 50 PKR for 7 days.');}
 private signText(mid:string,uid:string,ts:number){return['CrickX Prediction Entry','Prediction: '+mid,'User: '+uid,'Timestamp: '+ts,'By signing, I confirm this wallet belongs to me and should receive any CRX prize earned by these predictions.'].join('\n');}
 private score(p:any,role:'bat'|'bowl'){const n=pname(p).toLowerCase(),list=role==='bat'?BATTERS:BOWLERS,i=list.findIndex(x=>n===x||n.includes(x)||x.includes(n));let s=i>=0?2000-i*5:0;const pos=String(p?.position_name??p?.position?.name??'').toLowerCase();if(p?.isPlayingXI)s+=500;if(role==='bat'&&(pos.includes('bat')||pos.includes('wicket')))s+=200;if(role==='bowl'&&(pos.includes('bowl')||pos.includes('all')))s+=200;return s;}
 private pick(all:any[],role:'bat'|'bowl',exclude?:number){const map=new Map<number,any>();for(const p of all){const id=Number(p?.player_id??p?.id);if(Number.isFinite(id)&&id>0&&id!==exclude)map.set(id,{...p,player_id:id});}const c=[...map.values()].sort((a,b)=>this.score(b,role)-this.score(a,role));return c.find(p=>this.score(p,role)>=1950)||c[0]||null;}
 private questions(f:any,s:any):Q[]{const teams=arr(s?.teams),a=f.localteam??teams[0]??{id:f.localteam_id,name:'Home Team'},b=f.visitorteam??teams[1]??{id:f.visitorteam_id,name:'Away Team'};const ps=[...(teams[0]?.players??[]),...(teams[1]?.players??[]),...(f.lineup??[])].map((p:any)=>({...p,player_id:Number(p?.player_id??p?.id),team_id:Number(p?.team_id??p?.teamId??p?.team?.id),teamName:p?.teamName??p?.team?.name})).filter((p:any)=>Number.isFinite(p.player_id));const bat=this.pick(ps,'bat'),bowl=this.pick(ps,'bowl',bat?.player_id);const feat=(p:any)=>p?{id:Number(p.player_id),name:pname(p),teamId:Number(p.team_id),teamName:p.teamName??(Number(p.team_id)===Number(a.id??f.localteam_id)?a.name:b.name)}:null;return[
{id:'match-winner',kind:'MATCH_WINNER',title:'Match Winner',prompt:'Who will win this match?',options:[{value:'LOCAL',label:String(a.name??'Home Team')},{value:'VISITOR',label:String(b.name??'Away Team')},{value:'DRAW',label:'Tie / No Result'}]},
{id:'featured-bowler',kind:'BOWLER_WICKETS',title:(feat(bowl)?.name??'Featured bowler')+"'s Bowling Performance",prompt:'Will '+(feat(bowl)?.name??'this bowler')+' take 3 or more wickets in this match?',options:[{value:'YES',label:'YES'},{value:'NO',label:'NO'}],featuredPlayer:feat(bowl)},
{id:'featured-batter',kind:'BATTER_RUNS',title:(feat(bat)?.name??'Featured batsman')+"'s Batting Milestone",prompt:'Will '+(feat(bat)?.name??'this batsman')+' score 50 or more runs in this match?',options:[{value:'YES',label:'YES'},{value:'NO',label:'NO'}],featuredPlayer:feat(bat)},
{id:'powerplay-score',kind:'POWERPLAY_SCORE',title:'Powerplay Score (First 6 Overs)',prompt:'Will '+(a.name??'the home team')+' score 50 or more runs in the first 6 overs?',options:[{value:'YES',label:'YES (50+ Runs)'},{value:'NO',label:'NO (Under 50 Runs)'}],teamId:Number(a.id??f.localteam_id)},
{id:'match-sixes',kind:'TOTAL_SIXES',title:'Match Sixes Counter',prompt:'Will this match have 11 or more sixes?',options:[{value:'YES',label:'YES (11 or more sixes)'},{value:'NO',label:'NO (10 or fewer sixes)'}]}
];}
 private async ensure(fixtureId:number){const id='prediction_'+fixtureId;const ex=await this.read(id);if(ex)return ex;const f=await this.sportmonks.getFixture(fixtureId);const start=new Date(f.starting_at);if(Number.isNaN(start.getTime()))throw new BadRequestException('This match does not have a valid start time.');let squad:any=null;try{squad=await this.sportmonks.getFixtureSquads(fixtureId);}catch(e){this.logger.warn('Prediction squad unavailable: '+String(e));}const status=voided(f.status)?'VOID':terminal(f.status)?'LOCKED':liveNow(f)?'LOCKED':'OPEN';const data={id,sportmonksFixtureId:fixtureId,status,lockAt:start,questions:this.questions(f,squad),participantCount:0,predictionCount:0,poolAmount:0,fundingStatus:'PENDING',poolFundedAmount:0,fundingTxHash:null,fundingError:null,chainPredictionId:null,settlementStatus:'PENDING',settlementTxHash:null,settlementError:null,resultComputedAt:null,completedAt:status==='VOID'?new Date():null,createdAt:new Date(),updatedAt:new Date()};await this.mref(id).set(data,{merge:true});return(await this.read(id))??data;}
 private pp(f:any,teamId:number){const balls=Array.isArray(f.balls)?f.balls:[],map=new Map<number,number>();for(const r of [...(f.lineup??[]),...(f.batting??[])]){const p=Number(r?.player_id??r?.id),t=Number(r?.team_id);if(Number.isFinite(p)&&Number.isFinite(t))map.set(p,t);}const used=new Map<number,number>();let total=0;for(const ball of balls){if(Number(map.get(Number(ball?.batsman_id)))!==Number(teamId))continue;const inn=Number(ball?.inning??0),sc=ball?.score??{},n=used.get(inn)??0;if(n>=36)continue;total+=Number(sc.runs??0)+Number(sc.wide??0)+Number(sc.noball??0)+Number(sc.bye??0)+Number(sc.leg_bye??0);if(Number(sc.wide??0)===0&&Number(sc.noball??0)===0)used.set(inn,n+1);}return total;}
 private resolve(f:any,qs:Q[]){const bat=Array.isArray(f.batting)?f.batting:[],bowl=Array.isArray(f.bowling)?f.bowling:[],local=Number(f.localteam_id??f.localteam?.id),winner=f.winner_team_id==null?null:Number(f.winner_team_id),draw=Boolean(f.draw_noresult)||winner===null||!Number.isFinite(winner),sixes=bat.reduce((s:number,r:any)=>s+Number(r?.six_x??0),0);return qs.map(q=>{let c='NO';if(q.kind==='MATCH_WINNER')c=draw?'DRAW':winner===local?'LOCAL':'VISITOR';else if(q.kind==='BOWLER_WICKETS')c=Number(bowl.find((r:any)=>Number(r?.player_id)===Number(q.featuredPlayer?.id))?.wickets??0)>=3?'YES':'NO';else if(q.kind==='BATTER_RUNS')c=Number(bat.find((r:any)=>Number(r?.player_id)===Number(q.featuredPlayer?.id))?.score??0)>=50?'YES':'NO';else if(q.kind==='POWERPLAY_SCORE')c=this.pp(f,Number(q.teamId))>=50?'YES':'NO';else if(q.kind==='TOTAL_SIXES')c=sixes>=11?'YES':'NO';return{...q,correctAnswer:c};});}
 private payout(entries:any[],pool:number){const e=entries.filter(x=>Number(x.correctCount)>=3&&x.walletAddress).sort((a,b)=>Number(b.correctCount)-Number(a.correctCount)||String(a.submittedAt??'').localeCompare(String(b.submittedAt??'')));if(!e.length||pool<=0)return{winners:[],ranks:new Map<string,number>(),human:new Map<string,number>(),base:new Map<string,bigint>()};const groups=new Map<number,any[]>();for(const x of e)groups.set(Number(x.correctCount),[...(groups.get(Number(x.correctCount))??[]),x]);const share=[...groups.keys()].reduce((s,k)=>s+(SHARES[k]??0),0),total=BigInt(Math.round(pool*1e18)),base=new Map<string,bigint>();let allocated=0n;for(const k of [5,4,3].filter(x=>groups.has(x))){const g=groups.get(k)!,tier=(total*BigInt(SHARES[k]))/BigInt(share),each=tier/BigInt(g.length);let rem=tier-each*BigInt(g.length);for(const x of g){const v=each+(rem>0n?1n:0n);if(rem>0n)rem-=1n;base.set(x.id,v);allocated+=v;}}const ordered=[5,4,3].filter(x=>groups.has(x)).flatMap(x=>groups.get(x)!);for(let i=0,totalLeft=total-allocated;totalLeft>0n;i++,totalLeft--){const x=ordered[i%ordered.length];base.set(x.id,(base.get(x.id)??0n)+1n);}const ranks=new Map<string,number>();let pos=0,lastScore:number|null=null,lastRank=0;for(const x of e){pos++;const score=Number(x.correctCount),r=score===lastScore?lastRank:pos;ranks.set(x.id,r);lastRank=r;lastScore=score;}const human=new Map<string,number>();for(const [id,v]of base)human.set(id,Number(v)/1e18);return{winners:e,ranks,human,base};}
 private async batch(writes:Array<(b:WriteBatch)=>void>){let b=this.db.db.batch(),n=0;for(const w of writes){w(b);n++;if(n>=450){await b.commit();b=this.db.db.batch();n=0;}}if(n)await b.commit();}
 private async fund(match:any,fixture:any){const start=new Date(fixture.starting_at).getTime();if(!Number.isFinite(start)||Date.now()<start)return match;if(match.status==='OPEN'){await this.mref(match.id).update({status:'LOCKED',updatedAt:new Date()});match={...match,status:'LOCKED'};}const count=Number(match.participantCount||0);if(count<=0)return match;if(!this.onchain.isConfigured()){await this.mref(match.id).update({fundingStatus:'FAILED',fundingError:'CRX prediction pool contract is not configured.',updatedAt:new Date()});return match;}let chain=Number(match.chainPredictionId);if(!Number.isFinite(chain)||chain<=0){chain=(await this.onchain.createPrediction(Math.floor(start/1000))).chainPredictionId;await this.mref(match.id).update({chainPredictionId:chain,updatedAt:new Date()});}const f=await this.onchain.fundPredictionPool(chain,count);await this.mref(match.id).update({fundingStatus:f.skipped?'PENDING':'FUNDED',poolFundedAmount:f.totalPool,fundingTxHash:f.fundingTxHash??match.fundingTxHash??null,fundingError:null,updatedAt:new Date()});return(await this.read(match.id))??match;}
 private async settle(match:any,fixture:any){const id=match.id;if(this.busy.has(id))return;this.busy.add(id);try{if(voided(fixture.status)){if(Number(match.participantCount)>0&&Number(match.chainPredictionId)>0&&String(match.fundingStatus)==='FUNDED'&&this.onchain.isConfigured()){try{const r=await this.onchain.refundPrediction(Number(match.chainPredictionId));await this.mref(id).update({status:'VOID',settlementStatus:'VOID',settlementTxHash:r.txHash??null,completedAt:new Date()});}catch(e){await this.mref(id).update({status:'VOID',settlementStatus:'PENDING',settlementError:String(e).slice(0,1000)});}}else await this.mref(id).update({status:'VOID',settlementStatus:'VOID',completedAt:new Date()});return;}if(Number(match.participantCount)>0&&String(match.fundingStatus)!=='FUNDED'&&this.onchain.isConfigured()){try{match=await this.fund(match,fixture);}catch(e){await this.mref(id).update({fundingStatus:'FAILED',fundingError:String(e).slice(0,1000)});match=await this.read(id)??match;}}const qs=this.resolve(fixture,arr(match.questions));const es=await this.db.db.collection('predictionEntries').where('predictionMatchId','==',id).get();const entries=es.docs.map(d=>({id:d.id,...d.data()}));const as=await this.db.db.collection('predictionAnswers').where('predictionMatchId','==',id).get();const by=new Map<string,any[]>();for(const d of as.docs){const a:any={id:d.id,...(d.data() as any)};by.set(String(a.predictionEntryId),[...(by.get(String(a.predictionEntryId))??[]),a]);}const map=new Map(qs.map(q=>[q.id,q]));const aw:Array<(b:WriteBatch)=>void>=[],resolved:any[]=[];for(const e of entries){let count=0;for(const a of by.get(e.id)??[]){const q=map.get(String(a.questionId)),ok=Boolean(q&&a.selectedAnswer===q.correctAnswer);if(ok)count++;aw.push(b=>b.update(this.aref(a.id),{correctAnswer:q?.correctAnswer??null,isCorrect:ok,updatedAt:new Date()}));}resolved.push({...e,correctCount:count});}const pool=Number(match.poolFundedAmount||match.poolAmount||0),plan=this.payout(resolved,pool),ew:Array<(b:WriteBatch)=>void>=[];for(const e of resolved)ew.push(b=>b.update(this.eref(e.id),{correctCount:e.correctCount,rank:plan.ranks.get(e.id)??null,status:e.correctCount>=3?'WINNER':'NO_PRIZE',prizeWon:null,updatedAt:new Date()}));ew.push(b=>b.update(this.mref(id),{questions:qs,status:'RESULTS_READY',resultComputedAt:new Date(),settlementStatus:'PENDING',settlementError:null,updatedAt:new Date()}));await this.batch([...aw,...ew]);match=await this.read(id)??match;if(!plan.winners.length){if(Number(match.participantCount)>0&&Number(match.chainPredictionId)>0&&String(match.fundingStatus)==='FUNDED'&&this.onchain.isConfigured()){try{const r=await this.onchain.refundPrediction(Number(match.chainPredictionId));await this.mref(id).update({status:'COMPLETED',settlementStatus:'NO_WINNERS',settlementTxHash:r.txHash??null,completedAt:new Date()});}catch(e){await this.mref(id).update({settlementStatus:'PENDING',settlementError:String(e).slice(0,1000)});}}else await this.mref(id).update({status:'COMPLETED',settlementStatus:'NO_WINNERS',completedAt:new Date()});return;}if(!this.onchain.isConfigured()||!Number(match.chainPredictionId)){await this.mref(id).update({settlementStatus:'PENDING_ONCHAIN_CONFIGURATION',settlementError:'Prediction pool contract is not configured yet.'});return;}const payoutList=plan.winners.map(e=>({wallet:String(e.walletAddress),amountBaseUnits:String(plan.base.get(e.id)??0n)}));try{const r=await this.onchain.settleFinal(Number(match.chainPredictionId),payoutList);const writes:Array<(b:WriteBatch)=>void>=[];for(const e of plan.winners)writes.push(b=>b.update(this.eref(e.id),{prizeWon:Number(plan.human.get(e.id)??0),updatedAt:new Date()}));writes.push(b=>b.update(this.mref(id),{status:'COMPLETED',settlementStatus:'COMPLETED',settlementTxHash:r.finalPrizeTxHash,completedAt:new Date(),updatedAt:new Date()}));await this.batch(writes);}catch(e){await this.mref(id).update({settlementStatus:'PENDING',settlementError:String(e).slice(0,1000)});}}finally{this.busy.delete(id);}}
 private async progress(match:any){
  if(['COMPLETED','VOID'].includes(String(match.status))) return;
  const f=await this.sportmonks.getFixture(Number(match.sportmonksFixtureId),{forceLive:true});
  if(terminal(f.status)){
    await this.settle(match,f);
    return;
  }

  // Predictions lock on the same official LIVE signal used by match status,
  // not merely because the scheduled start time has passed.
  const isLive=liveNow(f);

  // Re-open stale prediction records created by the previous start-time-based
  // logic when Sportmonks still says the fixture has not started.
  if(match.status==='LOCKED'&&!isLive){
    await this.mref(match.id).update({status:'OPEN',updatedAt:new Date()});
    return;
  }

  if(isLive){
    try{await this.fund(match,f);}
    catch(e){this.logger.error('Prediction funding failed fixture='+match.sportmonksFixtureId+': '+String(e));}
  }
}
 async getPublic(fixtureId:number){let m=await this.ensure(fixtureId);await this.progress(m);m=await this.read(m.id)??m;const s=String(m.status),reveal=['RESULTS_READY','COMPLETED','VOID'].includes(s);return{matchId:m.id,fixtureId,status:s,lockAt:m.lockAt,participantCount:Number(m.participantCount||0),predictionCount:Number(m.predictionCount||0),poolAmount:Number(m.poolAmount||0),poolFundedAmount:Number(m.poolFundedAmount||0),fundingStatus:m.fundingStatus,settlementStatus:m.settlementStatus,chainPredictionId:m.chainPredictionId??null,questions:arr(m.questions).map((q:any)=>{const x:any={id:q.id,kind:q.kind,title:q.title,prompt:q.prompt,options:q.options,featuredPlayer:q.featuredPlayer??null,teamId:q.teamId??null};if(reveal&&q.correctAnswer)x.correctAnswer=q.correctAnswer;return x;}),entry:{predictionsPerMatch:FIVE,crxPerPrediction:TEN,totalCrxContribution:TOTAL,subscriptionRequired:true},canVote:s==='OPEN'};}
 async getMine(uid:string,fixtureId:number){const m=await this.ensure(fixtureId);const snap=await this.db.db.collection('predictionEntries').where('predictionMatchId','==',m.id).where('userId','==',uid).limit(1).get();if(snap.empty)return null;const e={id:snap.docs[0].id,...snap.docs[0].data()} as any;const a=await this.db.db.collection('predictionAnswers').where('predictionEntryId','==',e.id).get();return{id:e.id,walletAddress:e.walletAddress,stakeAmount:Number(e.stakeAmount??TOTAL),correctCount:e.correctCount??null,rank:e.rank??null,prizeWon:e.prizeWon!=null?Number(e.prizeWon):null,status:e.status,submittedAt:e.submittedAt,updatedAt:e.updatedAt,answers:a.docs.map(d=>({questionId:d.data().questionId,selectedAnswer:d.data().selectedAnswer,correctAnswer:d.data().correctAnswer??null,isCorrect:d.data().isCorrect??null}))};}
 async submit(uid:string,fixtureId:number,dto:SubmitPredictionDto){await this.sub(uid);let m=await this.ensure(fixtureId);await this.progress(m);m=await this.read(m.id)??m;if(m.status!=='OPEN')throw new ForbiddenException('Predictions are locked because this match has started or completed.');if(!Array.isArray(dto.answers)||dto.answers.length!==FIVE)throw new BadRequestException('You must answer all 5 predictions.');const qs=arr(m.questions) as Q[],map=new Map<string,string>();for(const x of dto.answers){const id=String((x as any)?.questionId??''),a=String((x as any)?.answer??'');if(!id||!a)throw new BadRequestException('Every prediction answer must include a question and selection.');if(map.has(id))throw new BadRequestException('Each prediction can only be answered once.');map.set(id,a);}for(const q of qs){const a=map.get(q.id);if(!a||!q.options.some(o=>o.value===a))throw new BadRequestException('Select an answer for: '+q.title);}const ts=Number(dto.walletMessageTimestamp),now=Math.floor(Date.now()/1000);if(!Number.isInteger(ts)||Math.abs(now-ts)>WINDOW)throw new ForbiddenException('The wallet confirmation expired. Please submit your predictions again.');let wallet:Address;try{wallet=getAddress(dto.walletAddress);}catch{throw new BadRequestException('Invalid wallet address.');}let ok=false;try{ok=await verifyMessage({address:wallet,message:this.signText(m.id,uid,ts),signature:dto.walletSignature as `0x${string}`});}catch{ok=false;}if(!ok)throw new ForbiddenException('Wallet signature could not be verified.');const entryId=m.id+'_'+uid,nowDate=new Date(),walletLock=this.wref(m.id,String(wallet).toLowerCase());let edited=false;await this.db.db.runTransaction(async tx=>{const mr=this.mref(m.id),er=this.eref(entryId),wr=walletLock;const ms=await tx.get(mr);const es=await tx.get(er);const ws=await tx.get(wr);if(!ms.exists)throw new NotFoundException('Prediction match not found.');const cur={id:ms.id,...ms.data()} as any;if(cur.status!=='OPEN')throw new ForbiddenException('Predictions are locked.');const ex=es.exists?({id:es.id,...es.data()} as any):null;edited=Boolean(ex);if(ex&&String(ex.walletAddress).toLowerCase()!==String(wallet).toLowerCase())throw new ForbiddenException('Your prediction wallet cannot be changed after the first submission.');if(!ex&&ws.exists&&String(ws.data()?.userId)!==uid)throw new ForbiddenException('This wallet is already used for predictions on this match.');tx.set(er,{userId:uid,predictionMatchId:m.id,walletAddress:String(wallet),stakeAmount:TOTAL,status:ex?.status??'SUBMITTED',correctCount:null,rank:null,prizeWon:null,submittedAt:ex?.submittedAt??nowDate,updatedAt:nowDate},{merge:true});tx.set(wr,{userId:uid,predictionMatchId:m.id,walletAddress:String(wallet),updatedAt:nowDate},{merge:true});const old=ex?await tx.get(this.db.db.collection('predictionAnswers').where('predictionEntryId','==',entryId)):null;if(old)for(const d of old.docs)tx.delete(d.ref);for(const q of qs){const ar=this.aref(entryId+'_'+q.id);tx.set(ar,{id:ar.id,predictionEntryId:entryId,predictionMatchId:m.id,questionId:q.id,selectedAnswer:map.get(q.id),correctAnswer:null,isCorrect:null,createdAt:nowDate,updatedAt:nowDate},{merge:true});}if(!ex)tx.update(mr,{participantCount:Number(cur.participantCount||0)+1,predictionCount:Number(cur.predictionCount||0)+FIVE,poolAmount:Number(cur.poolAmount||0)+TOTAL,updatedAt:nowDate});else tx.update(mr,{updatedAt:nowDate});});const final=await this.read(m.id);return{saved:true,edited,participantCount:Number(final?.participantCount||0),totalPool:Number(final?.poolAmount||0),totalCrxContribution:TOTAL,predictionsLockedAt:m.lockAt};}
 private async sweep(){try{const snap=await this.db.db.collection('predictionMatches').where('status','in',['OPEN','LOCKED','RESULTS_READY']).select('sportmonksFixtureId','status').get();for(const d of snap.docs){try{await this.progress({id:d.id,...d.data()});}catch(e){this.logger.error('Prediction sweep failed fixture='+d.data().sportmonksFixtureId+': '+String(e));}}}catch(e){this.logger.error('Prediction sweep failed: '+String(e));}}
}
